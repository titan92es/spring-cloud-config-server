server:
  port: 8888
 
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/titan92es/spring-cloud-config
          #username:
          #password:
          search-paths:
          - login*
          force-pull: true
          
          